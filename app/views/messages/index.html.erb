<% @messages.each do |message| %>
	<div class="col-md-4">
		<div class="message">
			<h2 class="message-title"><%= message.title %></h2>
			<br />
			<%= link_to "View Ad", message, class: "btn-custom" %>
		</div>
	</div>
<% end %>

